apiVersion: v1
kind: Service
metadata:
  name: zync-database
  namespace: 3scale-persistence
spec:
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  selector:
    app: zync-database