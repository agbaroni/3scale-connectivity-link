apiVersion: v1
kind: Service
metadata:
  name: system-database
  namespace: 3scale-persistence
spec:
  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306
  selector:
    app: system-database